name: Auto Commit

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch:

jobs:
  commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write   

    steps:
      - uses: actions/checkout@v3

      - name: Run commit script
        run: |
          git config user.email "anavatrim@hotmail.com"
          git config user.name "anavatrim"
          git commit --allow-empty -m "OOps a commit $(date +'%Y-%m-%d %H:%M:%S')"
          git push
